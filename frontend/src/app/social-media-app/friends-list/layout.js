export default function FriendsListLayout({ children }) {
    return (
        <div>
            <nav>
                {/* Add any navigation options here */}
            </nav>
            <main>{children}</main>
        </div>
    );
}
