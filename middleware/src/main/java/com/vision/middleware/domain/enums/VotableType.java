package com.vision.middleware.domain.enums;

/**
 * Enum representing different types of votable entities.
 */
public enum VotableType {
    /**
     * Represents a post that can be voted on.
     */
    POST,

    /**
     * Represents a reply that can be voted on.
     */
    REPLY
}
