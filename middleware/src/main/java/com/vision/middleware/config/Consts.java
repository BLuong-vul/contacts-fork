package com.vision.middleware.config;

/**
 * A class that holds defined constants used throughout the application.
 */
public class Consts {
    /**
     * Private constructor to prevent instantiation of the utility class.
     */
    private Consts() {}

    /**
     * The default session timeout in milliseconds (24 hours).
     */
    public static final int SESSION_TIMEOUT_MILLIS = 86400000; // 24 hrs
}
